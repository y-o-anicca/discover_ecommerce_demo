overwrite: true
schema: "../graph/schema.graphqls"
documents: "src/api/**/*.graphql"
generates:
  src/api/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
